# BentoML config
service: "service:iris_service"  # Same as the argument passed to bentoml
labels:
  owner: shibiraj
  stage: dev
include:
  - "*.py"  # A pattern for matching which files to include in the bento
python:
   packages:  # Additional pip packages required by the service
     - requests==2.31.0
     - scikit-learn==1.3.0

